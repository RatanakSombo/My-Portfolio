{
  "name": "portfolio-website-root",
  "version": "1.0.0",
  "description": "Root for portfolio website client and server",
  "scripts": {
    "client": "npm run dev --prefix client",
    "server": "npm run dev --prefix server",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
